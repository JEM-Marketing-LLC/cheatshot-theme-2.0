<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="theme-color" content="{{ settings.color_yellow }}">
    <link rel="canonical" href="{{ canonical_url }}">

    {%- if settings.favicon != blank -%}
      <link rel="icon" type="image/png" href="{{ settings.favicon | image_url: width: 32, height: 32 }}">
    {%- endif -%}

    <title>
      {{ page_title }}
      {%- if current_tags %} &ndash; {{ 'general.meta.tags' | t: tags: current_tags | join: ', ' }}{% endif -%}
      {%- if current_page != 1 %} &ndash; {{ 'general.meta.page' | t: page: current_page }}{% endif -%}
      {%- unless page_title contains shop.name %} &ndash; {{ shop.name }}{% endunless -%}
    </title>

    {%- if page_description -%}
      <meta name="description" content="{{ page_description | escape }}">
    {%- endif -%}

    {% render 'meta-tags' %}

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:ital,wght@0,700;0,800;0,900;1,900&family=Barlow:wght@400;600;700&display=swap"
    >

    <style>
      :root {
        --cream: {{ settings.color_cream }};
        --cream-dark: #F5F2EA;
        --cream-deep: #E6E1D5;
        --ink: {{ settings.color_ink }};
        --ink-mid: #737373;
        --red: {{ settings.color_red }};
        --gold: {{ settings.color_gold }};
        --yellow: {{ settings.color_yellow }};
        --page-width: {{ settings.page_width }}px;
        --radius: 6px;
        --radius-lg: 14px;
        --heading-scale: {{ settings.heading_scale | divided_by: 100.0 }};
        --font-display: 'Barlow Condensed', 'Arial Narrow', sans-serif;
        --font-body: 'Barlow', system-ui, -apple-system, sans-serif;
        {% if settings.show_hard_shadows %}
          --shadow-ink: 6px 6px 0 0 {{ settings.color_ink }};
          --shadow-ink-pressed: 3px 3px 0 0 {{ settings.color_ink }};
          --shadow-gold: 12px 12px 0 0 {{ settings.color_gold }};
        {% else %}
          --shadow-ink: 0 6px 20px rgba(0, 0, 0, 0.08);
          --shadow-ink-pressed: 0 2px 8px rgba(0, 0, 0, 0.1);
          --shadow-gold: 0 18px 44px rgba(0, 0, 0, 0.08);
        {% endif %}
      }
    </style>

    {{ 'theme.css' | asset_url | stylesheet_tag }}

    <script>
      window.CheatShot = {
        routes: {
          cart_add_url: '{{ routes.cart_add_url }}',
          cart_change_url: '{{ routes.cart_change_url }}',
          cart_url: '{{ routes.cart_url }}'
        },
        cartType: '{{ settings.cart_type }}',
        moneyFormat: {{ shop.money_format | json }}
      };
    </script>
    <script src="{{ 'theme.js' | asset_url }}" defer></script>

    {{ content_for_header }}
  </head>

  <body class="template-{{ template.name | handle }}">
    <a class="skip-to-content visually-hidden" href="#MainContent">
      {{ 'general.accessibility.skip_to_content' | t }}
    </a>

    {% sections 'header-group' %}

    <main id="MainContent" role="main">
      {{ content_for_layout }}
    </main>

    {% sections 'footer-group' %}

    {% render 'cart-drawer' %}
  </body>
</html>
