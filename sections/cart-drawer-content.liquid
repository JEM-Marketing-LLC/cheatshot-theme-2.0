{%- comment -%}
  Fetched via ?section_id=cart-drawer-content so the drawer can refresh
  its contents without a full page reload.
{%- endcomment -%}
<div data-drawer-content>
  {% render 'cart-drawer-body' %}
</div>

{% schema %}
{
  "name": "Cart drawer content",
  "settings": []
}
{% endschema %}
